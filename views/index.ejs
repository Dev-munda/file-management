<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Send OTP</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            padding-top: 40px;
        }
        .login-container {
            max-width: 450px;
            margin: 0 auto;
            background-color: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
        }
        .login-header {
            text-align: center;
            margin-bottom: 30px;
        }
        .login-icon {
            font-size: 3rem;
            color: #0d6efd;
            margin-bottom: 15px;
        }
        .btn-send-otp {
            width: 100%;
            padding: 12px;
            font-weight: 500;
            background-color: #0d6efd;
            border: none;
        }
        .btn-send-otp:hover {
            background-color: #0b5ed7;
        }
        .alert-danger {
            text-align: center;
            margin-bottom: 25px;
        }
        .login-description {
            text-align: center;
            color: #6c757d;
            margin-bottom: 30px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="login-container">
            <div class="login-header">
                <div class="login-icon">
                    <i class="fas fa-envelope"></i>
                </div>
                <h2>Secure Login</h2>
                <p class="login-description">We'll send a one-time password to your registered email</p>
            </div>

            <% if (message) { %>
                <div class="alert alert-danger">
                    <i class="fas fa-exclamation-circle me-2"></i><%= message %>
                </div>
            <% } %>

            <form action="/send-otp" method="POST">
                <button type="submit" class="btn btn-primary btn-send-otp">
                    <i class="fas fa-paper-plane me-2"></i> Send OTP
                </button>
            </form>

            <!-- <div class="text-center mt-4">
                <small class="text-muted">By continuing, you agree to our Terms of Service</small>
            </div>
        </div> -->
    </div>

    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>